---
phase: 02-config-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/gatus/config_flow.py
  - custom_components/gatus/const.py
  - custom_components/gatus/strings.json
  - custom_components/gatus/translations/en.json
  - tests/test_config_flow.py
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "User can add a Gatus instance by entering URL + optional API key + optional prefix via HA UI"
    - "Config Flow validates the URL is reachable; unreachable URL shows 'cannot_connect' error and does not save the entry"
    - "Config Flow rejects a URL already in use by another config entry (normalized duplicate check)"
    - "Entry title is set to the URL hostname; entry data contains url, api_key, prefix"
    - "Entity prefix defaults to 'gatus_' when left blank"
  artifacts:
    - path: "custom_components/gatus/config_flow.py"
      provides: "GatusConfigFlow.async_step_user + _validate_gatus_connection helper"
      exports: ["GatusConfigFlow"]
    - path: "custom_components/gatus/const.py"
      provides: "DEFAULT_PREFIX, MIN_SCAN_INTERVAL, MAX_SCAN_INTERVAL constants"
      contains: "DEFAULT_PREFIX"
    - path: "custom_components/gatus/strings.json"
      provides: "Config flow UI strings (step user + error keys)"
    - path: "custom_components/gatus/translations/en.json"
      provides: "English translations mirroring strings.json"
    - path: "tests/test_config_flow.py"
      provides: "Tests for async_step_user (happy path, bad URL, 401, duplicate)"
  key_links:
    - from: "GatusConfigFlow.async_step_user"
      to: "_validate_gatus_connection"
      via: "awaited call with session, url, api_key"
      pattern: "_validate_gatus_connection"
    - from: "_validate_gatus_connection"
      to: "GET /api/v1/endpoints/statuses"
      via: "aiohttp session.get(f'{url}/api/v1/endpoints/statuses')"
      pattern: "api/v1/endpoints/statuses"
    - from: "async_step_user"
      to: "async_set_unique_id / _abort_if_unique_id_configured"
      via: "normalized URL as unique_id for duplicate detection"
      pattern: "async_set_unique_id"
---

<objective>
Implement the Config Flow's core user-facing step: URL entry, API key, entity prefix, validation, and duplicate detection.

Purpose: Users can add a Gatus instance to HA without touching YAML; bad URLs and duplicates are caught before the entry is saved.
Output: Working `async_step_user`, shared `_validate_gatus_connection` helper, form strings, 4 passing test cases.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-config-flow/02-CONTEXT.md
@custom_components/gatus/__init__.py
@custom_components/gatus/config_flow.py
@custom_components/gatus/const.py
</context>

<interfaces>
<!-- Existing contracts the executor must respect. Do NOT deviate. -->

From custom_components/gatus/__init__.py:
```python
coordinator = GatusDataUpdateCoordinator(
    hass,
    url=entry.data["url"],
    api_key=entry.data.get("api_key"),
    scan_interval=entry.options.get("scan_interval", DEFAULT_SCAN_INTERVAL),
)
```
Config Flow MUST produce entry.data with keys: `url` (str), `api_key` (str | None), `prefix` (str).
Config Flow MUST produce entry.options with key: `scan_interval` (int, default 60).

From custom_components/gatus/const.py:
```python
DOMAIN = "gatus"
DEFAULT_SCAN_INTERVAL = 60  # seconds
```

HA Config Flow imports:
```python
from homeassistant.config_entries import ConfigFlow, ConfigFlowResult
from homeassistant.helpers.aiohttp_client import async_get_clientsession
from homeassistant.helpers import selector
import voluptuous as vol
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Validation helper + async_step_user (RED → GREEN)</name>
  <files>custom_components/gatus/config_flow.py, custom_components/gatus/const.py, tests/test_config_flow.py</files>
  <behavior>
    - Test 1 (happy path): user_input {url: "http://gatus.test", api_key: None, prefix: "gatus_"} → mock GET /api/v1/endpoints/statuses returns 200 → async_create_entry with data={url, api_key=None, prefix="gatus_"} and options={scan_interval=60}
    - Test 2 (cannot_connect): mock GET raises aiohttp.ClientError → form re-shown with errors={"base": "cannot_connect"}
    - Test 3 (invalid_auth): mock GET returns 401 → form re-shown with errors={"base": "invalid_auth"}
    - Test 4 (duplicate): same URL already configured as existing MockConfigEntry → async_abort(reason="already_configured")
    - Test 5 (custom prefix): user_input prefix="myprefix_" → entry.data["prefix"] == "myprefix_"
    - Test 6 (empty prefix): user_input prefix="" → entry.data["prefix"] == "gatus_" (default applied)
    - Test 7 (trailing slash stripped): url "http://gatus.test/" → stored as "http://gatus.test" (per D-07 normalization)
  </behavior>
  <action>
    1. Add to `const.py`: `DEFAULT_PREFIX = "gatus_"`, `MIN_SCAN_INTERVAL = 30`, `MAX_SCAN_INTERVAL = 300`.

    2. Write failing tests in `tests/test_config_flow.py` covering all 7 behaviors above. Use `aioclient_mock` fixture (from pytest-homeassistant-custom-component) for HTTP mocking and `MockConfigEntry` for duplicate-detection setup. Import `hass` fixture from HA test helpers.

       Test setup pattern:
       ```python
       from pytest_homeassistant_custom_component.common import MockConfigEntry
       from custom_components.gatus.const import DOMAIN
       ```

       For duplicate test: create a `MockConfigEntry(domain=DOMAIN, data={"url": "http://gatus.test", ...})` and call `entry.add_to_hass(hass)` before initiating the flow.

    3. Run `pytest tests/test_config_flow.py -x` — ALL tests MUST fail (RED).

    4. Implement in `config_flow.py`:

       **`_validate_gatus_connection(session, url, api_key) -> None`** (module-level async function, per D-02):
       - GET `{url}/api/v1/endpoints/statuses` with header `Authorization: Bearer {api_key}` if api_key is not None
       - 2xx → return (success)
       - 401 or 403 → raise `InvalidAuth`
       - `aiohttp.ClientError` or non-2xx-non-401/403 → raise `CannotConnect`
       - Use `asyncio.timeout(10)` (stdlib) for the request — not deprecated `async_timeout` module

       **Custom exceptions** (inner classes or module-level):
       ```python
       class CannotConnect(Exception): pass
       class InvalidAuth(Exception): pass
       ```

       **`GatusConfigFlow.async_step_user`** (replaces Phase 1 stub):
       - Schema: `vol.Schema({vol.Required("url"): selector.TextSelector(), vol.Optional("api_key"): selector.TextSelector(), vol.Optional("prefix", default=DEFAULT_PREFIX): selector.TextSelector()})`
       - On user_input:
         a. Normalize URL: strip trailing slash, `url.rstrip("/")`
         b. Apply prefix default: `prefix = user_input.get("prefix") or DEFAULT_PREFIX`
         c. Call `await _validate_gatus_connection(async_get_clientsession(hass), url, api_key)` — map exceptions to errors dict
         d. Duplicate check: `await self.async_set_unique_id(url.lower())` then `self._abort_if_unique_id_configured()`
         e. Title: `yarl.URL(url).host` (use stdlib `urllib.parse.urlparse(url).hostname` as fallback — no new dep)
         f. `return self.async_create_entry(title=title, data={"url": url, "api_key": api_key or None, "prefix": prefix}, options={"scan_interval": DEFAULT_SCAN_INTERVAL})`
       - On no user_input: show form

    5. Run `pytest tests/test_config_flow.py -x` — ALL tests MUST pass (GREEN).
    6. Run `ruff check custom_components/gatus/ && mypy custom_components/gatus/` — clean.
  </action>
  <verify>
    <automated>pytest tests/test_config_flow.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>7 tests pass; ruff and mypy clean; config_flow.py has _validate_gatus_connection and async_step_user fully implemented</done>
</task>

<task type="auto">
  <name>Task 2: Config Flow strings and translations</name>
  <files>custom_components/gatus/strings.json, custom_components/gatus/translations/en.json</files>
  <action>
    Create/update `strings.json` with Config Flow strings for the `step_user` form and error keys:

    ```json
    {
      "config": {
        "step": {
          "user": {
            "data": {
              "url": "Gatus URL",
              "api_key": "API Key (optional)",
              "prefix": "Entity ID prefix"
            },
            "data_description": {
              "url": "Base URL of your Gatus instance (e.g. https://status.example.com)",
              "api_key": "Leave blank if Gatus has no authentication configured",
              "prefix": "Prefix for entity object IDs (default: gatus_)"
            },
            "description": "Connect to your Gatus monitoring instance"
          }
        },
        "error": {
          "cannot_connect": "Unable to reach the Gatus instance. Check the URL and try again.",
          "invalid_auth": "Authentication failed. Check your API key.",
          "unknown": "Unexpected error. Check the logs."
        },
        "abort": {
          "already_configured": "A Gatus instance at this URL is already configured."
        }
      }
    }
    ```

    Create `translations/en.json` as an exact copy of `strings.json` (HA convention — both must exist).

    Ensure the `translations/` directory exists.
  </action>
  <verify>
    <automated>python -c "import json; s=json.load(open('custom_components/gatus/strings.json')); t=json.load(open('custom_components/gatus/translations/en.json')); assert s == t, 'mismatch'; print('OK')"</automated>
  </verify>
  <done>strings.json and translations/en.json exist with matching content; all Config Flow error keys present</done>
</task>

</tasks>

<verification>
- `pytest tests/test_config_flow.py -v` → all tests pass
- `ruff check custom_components/gatus/ && ruff format --check custom_components/gatus/` → clean
- `mypy custom_components/gatus/` → no errors
- `python -c "from custom_components.gatus.config_flow import GatusConfigFlow; print('import OK')"` → imports clean
</verification>

<success_criteria>
- Config Flow `async_step_user` fully implemented with validation and duplicate detection
- `_validate_gatus_connection` helper is shared (not duplicated) — reusable by Phase 2 Plan 02 Reconfigure flow
- All test cases (happy path, cannot_connect, invalid_auth, duplicate, custom prefix, empty prefix, trailing slash) pass
- strings.json and translations/en.json present and consistent
- ruff + mypy clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-flow/02-01-SUMMARY.md`
</output>
