---
phase: 01-core-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/gatus/const.py
  - custom_components/gatus/manifest.json
  - custom_components/gatus/__init__.py
  - pyproject.toml
  - .ruff.toml
  - hacs.json
autonomous: false
requirements:
  - POLL-04

must_haves:
  truths:
    - "DOMAIN constant equals 'gatus' and DEFAULT_SCAN_INTERVAL equals 60"
    - "manifest.json contains all required hassfest fields including documentation and issue_tracker"
    - "pyproject.toml configures pytest with asyncio_mode=auto and ruff with line-length=120"
    - "Dev dependencies are installed and ruff runs without error on the custom_components/gatus/ directory"
  artifacts:
    - path: "custom_components/gatus/const.py"
      provides: "DOMAIN, DEFAULT_SCAN_INTERVAL constants"
      contains: "DOMAIN = \"gatus\""
    - path: "custom_components/gatus/manifest.json"
      provides: "Integration metadata — all hassfest-required fields"
      contains: "\"domain\": \"gatus\""
    - path: "custom_components/gatus/__init__.py"
      provides: "Minimal stub so HA can load the package; placeholder for Phase 3 platform forwarding"
    - path: "pyproject.toml"
      provides: "pytest + ruff + mypy configuration"
      contains: "asyncio_mode"
    - path: ".ruff.toml"
      provides: "Ruff line-length and target-version config"
    - path: "hacs.json"
      provides: "HACS repository metadata"
  key_links:
    - from: "custom_components/gatus/__init__.py"
      to: "custom_components/gatus/const.py"
      via: "from .const import DOMAIN"
      pattern: "from .const import DOMAIN"

user_setup: []
---

<objective>
Create the static integration scaffold: constants, manifest, HACS metadata, tool configuration, and a minimal __init__.py stub. This plan produces the files that every subsequent plan imports or builds on.

Purpose: Establish the non-behavioral foundation — file layout, constants, and manifest — before any HTTP logic is written. The coordinator and __init__.py lifecycle (Plans 02 and 03) import from these artifacts.
Output: const.py, manifest.json, hacs.json, __init__.py stub, pyproject.toml, .ruff.toml, and installed dev dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/akentner/Projects/homeassistant-gatus-integration/.planning/PROJECT.md
@/home/akentner/Projects/homeassistant-gatus-integration/.planning/ROADMAP.md
@/home/akentner/Projects/homeassistant-gatus-integration/.planning/phases/01-core-scaffold/01-CONTEXT.md
@/home/akentner/Projects/homeassistant-gatus-integration/.planning/phases/01-core-scaffold/01-RESEARCH.md

<interfaces>
<!-- Phase 1 creates these constants consumed by Plans 02 and 03 -->
From custom_components/gatus/const.py (to be created):
  DOMAIN: str = "gatus"
  DEFAULT_SCAN_INTERVAL: int = 60  # seconds
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify dev package legitimacy before install</name>
  <what-built>
    The following packages are tagged [ASSUMED] because slopcheck was unavailable during research.
    Before installing, verify each is a legitimate, well-known ecosystem package.
  </what-built>
  <how-to-verify>
    For each package below, confirm it exists at the listed registry URL with a matching source repo.
    Expected: all are well-known tools with documented maintainers.

    1. pytest — https://pypi.org/project/pytest/ (source: github.com/pytest-dev/pytest)
    2. pytest-homeassistant-custom-component — https://pypi.org/project/pytest-homeassistant-custom-component/ (source: github.com/MatthewFlamm/pytest-homeassistant-custom-component)
    3. pytest-asyncio — https://pypi.org/project/pytest-asyncio/ (source: github.com/pytest-dev/pytest-asyncio)
    4. pytest-cov — https://pypi.org/project/pytest-cov/ (source: github.com/pytest-dev/pytest-cov)
    5. ruff — https://pypi.org/project/ruff/ (source: github.com/astral-sh/ruff)
    6. mypy — https://pypi.org/project/mypy/ (source: github.com/python/mypy)

    Each package: confirm the "Home Page" or "Source Code" link in PyPI matches the expected source repo above.
    Flag any discrepancy.
  </how-to-verify>
  <resume-signal>Type "verified" after confirming all packages, or describe any discrepancy found</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scaffold files and install dev dependencies</name>
  <files>
    custom_components/gatus/__init__.py,
    custom_components/gatus/const.py,
    custom_components/gatus/manifest.json,
    hacs.json,
    pyproject.toml,
    .ruff.toml
  </files>
  <read_first>
    - /home/akentner/Projects/homeassistant-gatus-integration/.planning/phases/01-core-scaffold/01-CONTEXT.md (decisions D-08, D-09, and the documentation field from RESEARCH Pitfall 1)
    - /home/akentner/Projects/homeassistant-gatus-integration/.planning/phases/01-core-scaffold/01-RESEARCH.md (§manifest.json example, §pyproject.toml test config, §Package Legitimacy Audit)
  </read_first>
  <action>
    Create directory custom_components/gatus/ and tests/.

    const.py: Define DOMAIN = "gatus" and DEFAULT_SCAN_INTERVAL = 60 (integer, seconds). These are the only two constants needed in Phase 1. Add a module docstring.

    __init__.py: Minimal stub — import DOMAIN from .const, declare GatusConfigEntry type alias as ConfigEntry[Any] (will be narrowed in Plan 03 when GatusDataUpdateCoordinator exists), and leave async_setup_entry and async_unload_entry as NotImplementedError stubs with docstrings explaining they are implemented in Plan 03. This stub is needed so Python can import the package. Do NOT add platform forwarding (no entities in Phase 1). Add a comment: "Phase 2: config_flow.py will be added; MockConfigEntry used in tests bypasses the flow".

    manifest.json: Per D-08 and D-09 (completing D-09 per RESEARCH Pitfall 1):
      domain: "gatus"
      name: "Gatus"
      codeowners: ["@akentner"]
      config_flow: true
      integration_type: "hub"
      iot_class: "cloud_polling"
      version: "0.1.0"
      requirements: []
      documentation: "https://github.com/akentner/homeassistant-gatus-integration"
      issue_tracker: "https://github.com/akentner/homeassistant-gatus-integration/issues"
    Do NOT include "quality_scale" (hassfest warns on custom integrations).

    hacs.json:
      name: "Gatus"
      homeassistant: "2025.1.0"
      hacs: "2.0.5"
    No "filename" field needed (standard folder-based distribution).

    pyproject.toml: Create with sections:
      [tool.pytest.ini_options]: asyncio_mode = "auto", testpaths = ["tests"]
      [tool.mypy]: python_version = "3.14", strict = true

    .ruff.toml: line-length = 120, target-version = "py314"

    Install dev dependencies (only after Task 1 human verification):
      uv add --dev pytest pytest-homeassistant-custom-component pytest-asyncio pytest-cov ruff mypy

    Create empty tests/__init__.py so pytest discovers the package.
  </action>
  <verify>
    <automated>
      cd /home/akentner/Projects/homeassistant-gatus-integration && python -c "import json; m=json.load(open('custom_components/gatus/manifest.json')); assert m['domain']=='gatus'; assert 'documentation' in m; assert 'issue_tracker' in m; print('manifest OK')"
    </automated>
    <automated>
      cd /home/akentner/Projects/homeassistant-gatus-integration && python -c "from custom_components.gatus.const import DOMAIN, DEFAULT_SCAN_INTERVAL; assert DOMAIN=='gatus'; assert DEFAULT_SCAN_INTERVAL==60; print('const OK')"
    </automated>
    <automated>
      cd /home/akentner/Projects/homeassistant-gatus-integration && ruff check custom_components/gatus/
    </automated>
  </verify>
  <acceptance_criteria>
    - custom_components/gatus/const.py exists and exports DOMAIN = "gatus" and DEFAULT_SCAN_INTERVAL = 60
    - manifest.json parses as valid JSON with keys: domain, name, codeowners, config_flow, integration_type, iot_class, version, requirements, documentation, issue_tracker
    - manifest.json does NOT contain "quality_scale"
    - hacs.json contains homeassistant: "2025.1.0" and hacs: "2.0.5"
    - pyproject.toml contains asyncio_mode = "auto" under [tool.pytest.ini_options]
    - .ruff.toml contains line-length = 120
    - ruff check custom_components/gatus/ exits 0
    - uv run python -c "import pytest" exits 0 (pytest installed in venv)
    - custom_components/gatus/__init__.py exists and contains "from .const import DOMAIN"
  </acceptance_criteria>
  <done>All scaffold files exist; manifest is complete with documentation and issue_tracker fields; ruff runs clean; dev dependencies installed in uv venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → dev machine | Package manager (uv/pip) fetches from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | manifest.json codeowners field | accept | GitHub username @akentner is public; no PII beyond what GitHub exposes |
| T-01-SC | Tampering | uv add (PyPI package install) | mitigate | blocking-human checkpoint (Task 1) requires manual PyPI registry verification before install; all 6 packages tagged [ASSUMED] |
| T-01-02 | Elevation of Privilege | SSRF via user-supplied URL | accept | URL validation is Phase 2 (Config Flow) work; no user input in this phase; documented in Phase 2 scope |
</threat_model>

<verification>
- const.py exports DOMAIN and DEFAULT_SCAN_INTERVAL with correct values
- manifest.json is valid JSON with all hassfest-required fields (including documentation, issue_tracker) and no quality_scale
- hacs.json present with correct homeassistant and hacs version fields
- ruff check exits 0 on custom_components/gatus/
- pytest --collect-only exits 0 (no test files yet, but test infrastructure is wired)
- uv run python -c "import homeassistant" — or at minimum pytest --version exits 0
</verification>

<success_criteria>
- Integration package directory custom_components/gatus/ exists with const.py, manifest.json, __init__.py
- All manifest.json hassfest-required fields present (RESEARCH Pitfall 1 resolved)
- Dev tool chain (ruff, pytest, mypy) installed and operable via uv run
- POLL-04 satisfied: DEFAULT_SCAN_INTERVAL = 60 defined in const.py
</success_criteria>

<output>
Create .planning/phases/01-core-scaffold/01-01-SUMMARY.md when done
</output>
